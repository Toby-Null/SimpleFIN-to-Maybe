# Server settings
PORT=3000
NODE_ENV=production

# Database connection
DB_HOST=db          # Use 'db' when running with Docker
DB_PORT=5432
DB_NAME=simplefin_maybe
DB_USER=postgres
DB_PASSWORD=yourpassword

# Docker-specific settings
USE_DOCKER_DB=true         # Set to false to use external PostgreSQL
DB_PORT_EXPOSE=5432        # External port for PostgreSQL container

# Session secret
SESSION_SECRET=your_session_secret_key